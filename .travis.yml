language: go

go:
  - 1.9.x

go_import_path: github.com/3hedgehogs/collectd-json-influxdb-proxy

install:
  - go get github.com/golang/lint/golint
  - go get github.com/golang/dep/cmd/dep

script:
  - dep ensure
  - go build .
  - go vet .
  - golint .
  - go test .
